import test from 'node:test';import assert from 'node:assert/strict';import { searchPlaces } from '../dashboard/lead-finder-server.js';test('missing key fails before request',async()=>{await assert.rejects(searchPlaces('Restaurants in Tüßling',{key:'',request:()=>{throw Error('unexpected');}}),/GOOGLE_PLACES_API_KEY/);});test('one page; websites last',async()=>{let options;const a=await searchPlaces('Restaurants in Tüßling',{key:'fake',request:async(_,o)=>{options=o;return{ok:true,json:async()=>({places:[{id:'web',displayName:{text:'A'},websiteUri:'https://example.org'},{id:'noweb',displayName:{text:'B'}}]})};}});assert.equal(JSON.parse(options.body).pageSize,20);assert.equal(a[0].name,'B');});