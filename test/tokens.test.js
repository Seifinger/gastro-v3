import test from 'node:test';import assert from 'node:assert/strict';import {contrast,deriveTokens} from '../src/tokens/index.js';test('black on white is 21:1',()=>assert.equal(contrast('#000000','#ffffff'),21));test('CTA contrast and deterministic archetype',()=>{const t=deriveTokens({kueche:'bayerisch'});assert.equal(t.archetype,'traditionell');assert.ok(contrast(t.color.accent,t.color.ctaText)>=7);});